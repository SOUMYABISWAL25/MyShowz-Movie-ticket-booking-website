{
  "name": "myshowz-movie-ticket-booking-website",
  "version": "1.0.0",
  "scripts": {
    "start": "npx serve .",
    "build:js": "esbuild assets/js/admin-upload.js --bundle --outfile=assets/js/bundles/admin-upload.js --format=esm && esbuild assets/js/movies-download.js --bundle --outfile=assets/js/bundles/movies-download.js --format=esm && esbuild assets/js/admin-dashboard.js --bundle --outfile=assets/js/bundles/admin-dashboard.js --format=esm && esbuild assets/js/index-movies.js --bundle --outfile=assets/js/bundles/index-movies.js --format=esm",
    "build": "npm run build:js && mkdir -p dist && cp -r assets dist/ && cp *.html dist/"
  },
  "dependencies": {
    "aws-amplify": "^6.15.7",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "@aws-amplify/backend": "^1.16.1",
    "@aws-amplify/backend-cli": "^1.8.0",
    "aws-cdk-lib": "^2.204.0",
    "constructs": "^10.4.2",
    "esbuild": "^0.25.11",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}