{
  "name": "myshowz-movie-ticket-booking-website",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp -r assets dist/ && cp *.html dist/"
  },
  "dependencies": {
    "serverless-http": "^3.2.0"
  }
}
